package com.example.estudos.Funções

/*

//EXEMPLO DO CALCULO DE GORJETA ONDE RECEBE OS VALORES DA MAINACTIVITY NA TELA SECUNDARIA


TELA PRINCIPAL


val intent = Intent(this,nomeDaTelaQuevaireceber::class.java)

intent.apply{

    putExtra(totalTable, totalTable)
    putExtra(numPessoas, NumofPeopleSelected)
    putExtra(percentage, percentage)
    putExtra(totalAmount, totalEithTips
}
startActivity(intent)

}*/


/*
TELA SECUNDARIA QUE IRA RECEBER OS DADOS CALCULADOS NA PRIMEIRA TELA DE GORJETA/RESULTADO

        val totalTable = intent.getFloatExtra("chave Nome dentro do putExtra, 0.0f")// inserir o valor para float assim pois se der erro ele devolve de qualquer forma algo
        val numPessoas = intent.getIntExtra("chave Nome dentro do putExtra", 0)
        val percentage = intent.getArrayExtra("chave Nome dentro do putExtra, 0")
        val totalAmount = intent.getFloatExtra("chave Nome dentro do putExtra, 0.0f")*/

/*

    binding.IdDaSegundaTela.textOuTipo = variavelDOIntentGet______Extra.toStraing()

        // para voltar para a tela anterior

        binding.botao.SETONCLICKLISTERN{
            FINISH()
        }*/
